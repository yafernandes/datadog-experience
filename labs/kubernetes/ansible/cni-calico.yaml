# https://docs.projectcalico.org/getting-started/kubernetes/self-managed-onprem/onpremises
- name: Deploy CNI
  hosts: master
  gather_facts: no
  tasks:
    - name: Deploy Calico
      become: yes
      shell: kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.25.1/manifests/calico.yaml --kubeconfig=/etc/kubernetes/admin.conf
